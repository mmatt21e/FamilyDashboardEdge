{
  "name": "family-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A private PWA dashboard for one family.",
  "scripts": {
    "test": "node --test test/logic.test.js",
    "test:browser": "node --test test/smoke.test.js",
    "test:all": "npm test && npm run test:browser",
    "serve": "python3 -m http.server 8080"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
